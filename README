Requisitos

TODOS:
-crear inputs para duration & delay & trigger

    1-Crear un componente aparte en el PanelBody para todos los SelectControl (en select-list-editor.js)

    Name AAA (Crear un input para todos los nombres)
    Delay 1000
    Duration 2000
    TimingFunction (ease, linear, ease-in, ease-out, ease-in-out, cubic-bezier(n,n,n,n))
    AnimationFillMode  (none, forwards, backwards, both)
    TRIGGER (hover, click, IntersectionObserver)

    2-Crear una funcion para agregar las variables iniciales en setting-attribute-default-value.js usando un input




    3-Crear la funcion que genera el codigo (en save-data.js)  A-1000-2000-A-A
    4-decodificar el codigo (usar un return) (en searchClassWithCode.js)
    5-Agregar los Values de las properties correspondientes (en searchClassWithCode.js)

-organizar todos los inputs / components - PanelBodyIntern tambien Input??
